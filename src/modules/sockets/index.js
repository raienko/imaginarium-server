
const addSocket = (user, socket) => {

};

const sendMessage = async (user, data) => {

};

const deleteSocket = async (user) => {

};

module.exports = {
  addSocket,
  sendMessage,
  deleteSocket,
}
